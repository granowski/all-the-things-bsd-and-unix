[global]
   workgroup = WORKGROUP
   dns proxy = no
   log file = /var/log/samba/log.%m
   max log size = 1000
   syslog = 0
   panic action = /usr/share/samba/panic-action %d
   server role = standalone server
   passdb backend = tdbsam
   obey pam restrictions = yes
   unix password sync = yes
   passwd program = /usr/bin/passwd %u
   passwd chat = *Enter\snew\s*\spassword:* %n\n *Retype\snew\s*\spassword:* %n\n *password\supdated\ssuccessfully* .
   pam password change = yes
   map to guest = bad user
   usershare allow guests = yes

[homes]
   comment = Home Directories
   browseable = yes 
   read only = no 
   create mask = 0700
   directory mask = 0700
   valid users = %S

[public]
  comment = Public data share for data drop
  path = /public
  writable = yes
  guest ok = yes
  guest only = no
  force create mode = 755
  force directory mode = 755

[granowski-data]
  comment = The raid disk of data
  path = /mnt/bitwave
  writable = yes
  guest ok = no 
  guest only = no
  valid users = granowski
  force create mode = 755
  force directory mode = 755

#[hornet]
#  comment = Data on exFAT pink hornet raid
#  path = /mnt/hornet
#  writable = yes
#  guest ok = no
#  guest only = no
#  valid users = granowski
#  force create mode = 755
#  force directory mode = 755

